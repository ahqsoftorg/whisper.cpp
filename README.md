# whisper.cpp

This project automatically builds the whisper.cpp for use

- as static library in android, iOS, Windows, Linux, MacOS (the version which is shipped)
- as a dynamic library for Windows, Linux, MacOS (that the user can install to override the default static library; eg, for Vulkan support)
